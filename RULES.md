# Глобальные правила Antigravity

1. **Перезапуск программы**: Ассистент НИКОГДА не должен перезапускать программу (сервер, pm2 и т.д.) самостоятельно. Это делает только пользователь.
2. **Завершение задачи**: По окончании работы ассистент обязан:
   - Написать фразу: "Я закончил".
   - Подробно рассказать, что именно было сделано.
   - Указать команды, которые пользователю нужно выполнить для запуска или проверки.
3. **Язык общения**: Все разговоры, объяснения и инструкции должны быть на русском языке.
4. **Стек технологий**: Использовать только JavaScript, Node.js, Gemini API. Никогда не использовать OpenAI.

---

## 💡 Как создавать файлы для лучшего понимания Gemini

Чтобы Gemini-ассистент (такой как я) мог максимально эффективно и быстро работать с вашим кодом, рекомендуется придерживаться следующих правил при структурировании файлов и написании кода.

### 1. Понятные и предсказуемые имена

*   **Файлы:** Имя файла должно четко отражать его содержимое.
    *   ✅ `databaseConnection.js`, `userAuthentication.js`
    *   ❌ `utils.js`, `data.js`, `misc.js`
*   **Функции:** Имя функции должно описывать, что она делает.
    *   ✅ `function getUserById(userId)`
    *   ❌ `function getData(id)`
*   **Переменные:** Имена переменных должны быть понятными без дополнительных комментариев.
    *   ✅ `const userName = 'John';`
    *   ❌ `const u_name = 'John';`

### 2. Принцип единой ответственности (Single Responsibility)

Каждый файл (модуль) и каждая функция должны выполнять одну, четко определенную задачу.

*   **Пример:**
    *   `utils/emailService.js`: всё, что связано с отправкой email.
    *   `handlers/userHandler.js`: всё, что связано с обработкой HTTP-запросов для пользователей (создание, удаление).
    *   `config/database.js`: только конфигурация и подключение к базе данных.

### 3. Конфигурация в `.env` файлах

Никогда не храните "секреты" (API ключи, пароли, токены) и переменные конфигурации (порты, домены) прямо в коде.

*   **Используйте файл `.env`** для хранения всех конфигурационных переменных.
*   **Добавьте файл `.env.example`** в репозиторий. Он должен быть копией `.env`, но со значениями-плейсхолдерами (например, `API_KEY=your_api_key_here`). Это помогает другим разработчикам (и мне) понять, какие переменные нужны для запуска проекта.

### 4. Комментарии: Объясняйте "Почему", а не "Что"

Код должен быть самодокументируемым. Комментарии нужны только для объяснения сложной, неочевидной или нестандартной логики.

*   **Хороший комментарий (объясняет "почему"):**
    ```javascript
    // Мы используем кэширование, чтобы избежать повторных запросов к API
    // и снизить нагрузку на сервер.
    const cachedData = cache.get(cacheKey);
    ```
*   **Плохой комментарий (объясняет "что"):**
    ```javascript
    // Увеличиваем счетчик на 1
    i++;
    ```

### 5. Документация функций (JSDoc)

Используйте блоки комментариев JSDoc перед функциями. Это один из самых полезных источников информации для меня.

*   **Пример:**
    ```javascript
    /**
     * Отправляет приветственное сообщение пользователю по email.
     * @param {string} userId - ID пользователя для поиска email.
     * @param {string} message - Текст сообщения.
     * @returns {Promise<boolean>} - Возвращает true в случае успеха, false в случае ошибки.
     */
    async function sendWelcomeEmail(userId, message) {
      // ... логика функции
    }
    ```

### 6. Постоянная структура проекта

Придерживайтесь единой и логичной структуры папок. Это позволяет быстро находить нужные файлы.

*   **Пример структуры:**
    ```
    /
    ├── config/         # Файлы конфигурации (база данных, API)
    ├── handlers/       # Обработчики роутов (Express, Fastify)
    ├── services/       # Бизнес-логика (работа с данными, внешние API)
    ├── utils/          # Вспомогательные функции (форматирование, валидация)
    ├── tests/          # Тесты
    ├── public/         # Статические файлы (CSS, изображения)
    └── index.js        # Точка входа в приложение
    ```