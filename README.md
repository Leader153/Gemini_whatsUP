gemini_salesAgent
//node tmp_chroma_query.js
//проверка того что есть в хромаДБ
проверь что chromadb включен
переход от бота к боту
На Windows PM2 часто требует запуска терминала
от имени Администратора!!!
Откройте командную строку (терминал).
   2. Выполните команду, чтобы найти запущенные процессы бота (они
      называются node.exe):

tasklist | findstr "node.exe"

Чтобы принудительно завершить их все одной командой, я сейчас
  выполню следующую команду:

  taskkill /F /IM node.exe /T

   * /F — принудительно завершить процесс.
   * /IM node.exe — найти все процессы с именем node.exe.
   * /T — завершить также и дочерние процессы.
Очистите состояние PM2:
powershell
pm2 kill

pm2 start ecosystem.config.js

---

## 🚀 Процесс развертывания: от локальной разработки до VPS

Это пошаговое руководство объясняет, как перенести проверенные изменения из локальной ветки (`testWatsapLocal5307`) в стабильную ветку `VPS`.

### Шаг 1: Убедитесь, что локальная ветка готова

Перед началом убедитесь, что все изменения в ветке `testWatsapLocal5307` полностью протестированы и закоммичены.

1.  **Проверьте статус**, чтобы убедиться, что нет незакоммиченных изменений:
    ```bash
    git status
    ```
2.  Если есть изменения, **создайте коммит**:
    ```bash
    git add .
    git commit -m "Ваше сообщение о коммите"
    ```

### Шаг 2: Обновите вашу локальную ветку (Хорошая практика)

Чтобы избежать конфликтов слияния в будущем, рекомендуется обновить вашу рабочую ветку последними изменениями из `main`.

1.  **Переключитесь на вашу рабочую ветку** (если вы не в ней):
    ```bash
    git checkout testWatsapLocal5307
    ```
2.  **Загрузите изменения** из `main` в вашу ветку:
    ```bash
    git pull origin main
    ```
    *Если возникнут конфликты, Git попросит вас разрешить их. После разрешения создайте новый коммит.*

### Шаг 3: Перенос изменений в ветку `VPS`

Теперь, когда ваша рабочая ветка готова, можно переносить изменения в `VPS`.

1.  **Переключитесь на ветку `VPS`**:
    ```bash
    git checkout VPS
    ```
2.  **Обновите ветку `VPS`**, чтобы она содержала самые последние изменения с сервера:
    ```bash
    git pull origin VPS
    ```
3.  **Слейте (merge) вашу рабочую ветку** (`testWatsapLocal5307`) в `VPS`:
    ```bash
    git merge testWatsapLocal5307
    ```
    *Эта команда применяет все коммиты из `testWatsapLocal5307` в `VPS`. Если возникнут конфликты, их нужно будет разрешить вручную и создать коммит слияния.*

### Шаг 4: Отправка изменений на удаленный сервер

После успешного слияния без конфликтов, отправьте обновленную ветку `VPS` на удаленный репозиторий (GitHub, GitLab и т.д.).

```bash
git push origin VPS
```

### Шаг 5: Развертывание на сервере VPS

Последний шаг — это обновление кода непосредственно на вашем VPS-сервере.

1.  **Подключитесь к вашему VPS** (например, через SSH).
2.  **Перейдите в папку с проектом**.
3.  **Загрузите последние изменения** из ветки `VPS`:
    ```bash
    git pull origin VPS
    ```
4.  **Перезапустите приложение**, чтобы применить изменения. Если вы используете `pm2`, команда будет такой:
    ```bash
    pm2 restart all
    ```

После выполнения этих шагов все ваши проверенные изменения будут работать на сервере VPS.